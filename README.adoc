# Vinyl Master - Vinyl Collection Management System

## Overview

Vinyl Master is a JavaFX desktop application for managing vinyl record collections. It provides an intuitive interface for cataloging, organizing, and tracking vinyl records with features like duplicate detection, genre management, and detailed metadata storage.

## Prerequisites

- **Java 21** (or later)
- **Gradle 8.11+** (wrapper included)

## Project Structure

This is a multi-module Gradle project following the Model-View-Controller (MVC) architectural pattern:

```
vinylmaster/
├── src/
│   ├── model/                          # Model module (data layer + business logic)
│   │   └── src/main/java/at/htl/leonding/vinylmaster/
│   │       ├── model/                 # Entity classes (Vinyl, Genre)
│   │       ├── repository/            # Data access layer (file-based JSON)
│   │       ├── service/               # Business logic layer
│   │       └── config/                # Configuration
│   ├── view/                           # View module (JavaFX UI resources)
│   │   └── src/main/resources/
│   │       ├── fxml/                 # FXML view definitions
│   │       └── images/               # UI assets
│   └── controller/                     # Controller module (UI orchestration)
│       └── src/main/java/at/htl/leonding/vinylmaster/
│           └── ui/
│               ├── controller/       # JavaFX controllers
│               ├── image/            # Image service
│               └── Main.java         # Application entry point
├── data/                               # Local data storage (auto-created, excluded from git)
│   ├── vinyls.json                   # All vinyl records
│   └── genres.json                   # Genre list
├── .gitignore
└── build.gradle.kts                  # Root build configuration
```

## Setup Instructions

### 1. Clone the Repository

```bash
git clone <repository-url>
cd 2526-3bhif-syp-project-vinyl-master
```

### 2. Build the Project

```bash
./gradlew build
```

This will:
- Compile backend and frontend modules
- Run all unit tests
- Create distribution packages

### 3. Run the Application

```bash
./gradlew :controller:run
```

Or build and run the standalone JAR:

```bash
./gradlew :controller:shadowJar
java -jar src/controller/build/libs/frontend-1.0.0-all.jar
```

The application will automatically create a `data/` directory to store all vinyl records as JSON files.

## Development

### Running Tests

```bash
# Run all tests
./gradlew test

# Run only model tests
./gradlew :model:test

# Run with detailed output
./gradlew test --info
```

### Data Storage

All vinyl records and genres are stored locally in JSON format:
- **Location:** `data/` directory (relative to application)
- **Files:**
  - `data/vinyls.json` — All vinyl records
  - `data/genres.json` — Genre list
- **Format:** Pretty-printed JSON (human-readable, easy to inspect)

The `data/` directory is automatically created on first application start.

## Features

### Current Features (v1.0.0)

- ✅ Add vinyl records with metadata (title, artist, genre, year, price)
- ✅ View collection in a scrollable list
- ✅ Frontend validation (required fields, data types)
- ✅ Backend validation (duplicate detection, data integrity)
- ✅ Genre dropdown with user-definable genres
- ✅ Price tracking in Euros
- ✅ Duplicate prevention (same title + artist + year)

### Validation Rules

**Required Fields:**
- Title
- Artist
- Year (must be between 1900-2100)

**Optional Fields:**
- Genre (user-definable dropdown)
- Price (must be non-negative if provided)

### Architecture

**Backend (Service + Repository Pattern):**
- `Repository Layer`: File-based JSON persistence
- `Service Layer`: Business logic including validation and duplicate checking

**Frontend (JavaFX + FXML):**
- Model-View-Controller pattern
- FXML for UI layout
- On-blur validation for immediate user feedback
- Modal overlay for add vinyl form

## Technology Stack

- **Language**: Java 21
- **UI Framework**: JavaFX 23
- **Persistence**: Local JSON files (Jackson)
- **Build Tool**: Gradle 8.11
- **Testing**: JUnit 5

## Documentation

For detailed system specifications and project documentation, visit:
https://2526-3bhif-syp.github.io/2526-3bhif-syp-project-vinyl-master/

## Troubleshooting

**Application won't start:**
- Ensure Java 21 is installed: `java --version`
- Check Gradle build: `./gradlew build --info`
- Ensure write permissions in the application directory (for `data/` folder creation)

**Tests failing:**
- Run tests with: `./gradlew test --info`
- Check test output for specific error messages

## License

This project is developed as part of the HTL Leonding System Engineering course.

---

**Development Team**: 3BHIF SYP 2025/26  
**Institution**: HTL Leonding
   
